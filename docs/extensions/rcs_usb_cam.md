# RCS USB Cam Extension

This extension provides support for generic USB webcams in RCS.

## Installation

```shell
pip install rcs-usb-cam
```

For local development:

```shell
pip install -ve . --no-build-isolation
pip install -ve extensions/rcs_usb_cam
```
